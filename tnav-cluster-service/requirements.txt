# The shared agent core (mas_agent_kit: sessions, tools, agent_result, Activity client) is not installed:
# it is the repo folder ../mas-agent-kit, put on sys.path by app/__init__.py. Its needs (fastapi, filelock) are below.
fastapi>=0.115.14,<0.116
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
python-dotenv>=1,<2
python-multipart>=0.0.9,<1.0
# Cross-platform advisory locks for disk-backed session state (Windows + Unix) — used by mas_agent_kit.SessionStore.
filelock>=3.15,<4.0
# SSH transport to the hydrodynamic cluster: exec for commands, SFTP for uploading the new schedule file.
# Pure-python paramiko + cryptography wheels exist for Windows / Python 3.11-3.13, nothing is built from source.
paramiko>=3.4,<4.0
