# Task: Единый write-path ответа и внешний resume (Фаза 1.3)

Read first: `AGENTS.md` (весь), `MAS_REFACTORING_PLAN.md` §2.2 H2/H6, §3 п.10, §4 Фаза 1; правила `.cursor/rules/mas-activity-service.mdc`, `n8n-templates.mdc`.

## Goal

Activity `/answer` больше не пишет `hitl.answers` и не эмитит `hitl.answered`. Он сохраняет файлы и зовёт оркестратор `action=resume` с сырым `{choice,text,label,files}` и `source=human`. Нормализация, журнал и событие — только в оркестраторе. `choice` с кнопки принимается как есть; свободный текст при вопросе с вариантами интерпретирует второй LLM-проход (тот же Qwen + Structured Output) в `{decision, confidence}`; при `confidence < 0.8` — переспрос прозой. `parseKeepRemove` / `parse_keep_remove` удаляются. `resume` принимает `source: human|agent|system` и `task_id` (путь долгого агента, §3 п.10). Структурный smoke машинности по сгенерированному JSON.

## Out of scope

- Фаза 2: `SYSTEM` оркестратора, `ROUTE`/`MERGE` bucket'ы, `agent_registry.invoke`, RAG без regex, `plan_update` в UI, `echo_agent`.
- `n8n/templates/retired/`, goldens, wipe кейсов.
- Python Excel / Schedule Builder, кроме тестов, если они читали `parse_keep_remove`.

## Where

- `mas-activity-service/app/cases_api.py` — `/answer` не мутирует hitl; `_invoke_resume`; `/run` с `action=resume` + `source=agent|system`.
- `n8n/templates/mas_state_utils.py` — удалить `parseKeepRemove`; `normalizeHitlAnswer` / `readUnlistedWellsPolicy` только `choice` / `unlisted_wells_policy`.
- `mas-activity-service/app/state_shape.py` — зеркало Python.
- `n8n/templates/generate_mas_orchestrator.py` — `source` в Normalize; resume human/agent/system; ветка интерпретации; регенерация.
- Тесты: `test_cases_api.py`, `test_state_shape.py`, `n8n/tests/mas-orchestrator-smoke.js`, новый `n8n/tests/human-text-structure-smoke.js`.
- `docs.md` §1.3; `MAS_REFACTORING_PLAN.md` H2/H6 + Ревизия 7.

## Contracts to respect

- Кнопка HITL: `{choice, text, label}` — без LLM.
- Тексты инженеру — русская проза (`looksMachineText` / `human_text_problems`).
- n8n 2.30.8: интерпретация — `chainLlm` 1.9 + `outputParserStructured` 1.3 (как Verify completion), не новый тип узла, не `toolHttpRequest`.
- Двойники Python/JS меняются вместе.

## Evidence to start from

- Activity `/answer` пишет `hitl.answers` и зовёт `action=step`; оркестратор `resume` живёт отдельно (`reconcileLedgerAnswers`).
- `parseKeepRemove` в `mas_state_utils.py` и `parse_keep_remove` в `state_shape.py`.

## Done when

- [x] `grep -rn parseKeepRemove n8n/templates mas-activity-service` пуст (кроме retired).
- [x] `/answer` не меняет `hitl.answers`; оркестратор `resume` пишет журнал.
- [x] Свободный текст без `choice` при вариантах → interpret; низкая уверенность → переспрос.
- [x] `POST /cases/{id}/run` `action=resume` `source=agent` не идёт в HITL-ветку.
- [x] `python3 scripts/mas_gate.py` GREEN; `--live` GREEN 6/6 (`CASE-6a9ee8f4` … `CASE-6a9eea8f`); interpret-ветка — `CASE-6a9ee706-8fe25c` (свободный текст), `CASE-6a9eeaed-1e839f` (переспрос).
- [x] План: H2/H6 ✅, Ревизия 7, `docs.md` §1.3.

## Forbidden

- regex по свободному ответу человека вместо LLM; домен в промпте оркестратора.
- ручная правка workflow JSON; `toolHttpRequest`; `$env`.
- машинный текст инженеру; инженер пишет `.INC`/JSON.
- переписывать goldens; ослаблять live-гейт.

## Report format

Файлы и счётчики; id шести live-кейсов; таблица `mas_gate.py`; что не сделано и почему.
