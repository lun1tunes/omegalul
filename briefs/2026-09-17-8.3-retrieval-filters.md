# Task: Фильтры RAG из инвентаря, не из regex (фаза 8.3 / R13)

Read first: `AGENTS.md` (весь), `MAS_REFACTORING_PLAN.md` §2.2 R13, §4 8.3, `.cursor/rules/mas-working-contract.mdc`, `mas-llm-first-no-domain-hardcode.mdc`, `n8n-templates.mdc`.

## Goal

Цитата плана §4 / R13: Excel `task_patterns` из `inspect.tables[].kind` и `expected_output.datasets[].name`; Schedule `keyword_families` из `inspect.keywords_present` ∪ keyword'ов `expected_output`/наборов; текст задачи — только в `query`. Regex-констант в спеках нет.

## Out of scope

- 8.4 `retrieve_knowledge` в промпте Builder, `kb_calls ≥ 1`
- 8.5–8.7 корпус, оркестратор, ingest/UI
- удаление `n8n_agent`
- SYSTEM оркестратора, regex-роутеры, goldens

## Where

- `n8n/templates/agents/excel_extractor.py` `retrieval_filters_js`
- `n8n/templates/agents/schedule_builder.py` `retrieval_filters_js`
- `excel-agent-tools/app/agent.py` `compact_inspect` — отдать `tables[].kind`
- `n8n/tests/excel-extractor-agent-smoke.js`, `schedule-builder-agent-smoke.js`
- `excel-agent-tools/tests/test_workflow_contracts.py`, `test_agent.py`
- regen: `python3 scripts/mas_gate.py --only regen` (или generate excel + schedule + `generate_schedule_workflows.py` последним)

## Contracts to respect

Текст задачи не выбирает инструмент и не наполняет `keyword_families`/`task_patterns`. Allowlist SCHEDULE — только членство, не regex по русской постановке. Ключ `error` в ответе инструмента запрещён.

## Evidence to start from

R13: `excel_extractor.py` `/дат|ввод|commission/`; `schedule_builder.py` `blob.match(/\b[A-Z]…/)` по handoff. Live 12/12 ревизии 50 на старых фильтрах (`CASE-6aac04eb-c10811`).

## Done when

- [x] Prepare smoke: фильтры из инвентаря; WCONPROD в тексте без `keywords_present` не попадает в `keyword_families`
- [x] в `retrieval_filters_js` нет `.test(low)` / `blob.match`
- [x] `python3 scripts/mas_gate.py` GREEN
- [x] `python3 scripts/mas_gate.py --live` GREEN 12/12, `rag_empty: 0`
- [x] план ревизия 51; шапка / §3 / §4 / §2.7 / «Ближайшие шаги» = код

## Forbidden

- regex-роутеры; домен в оркестраторе; правка JSON руками; `toolHttpRequest`; `$env`/`require`
- порог `kb_calls ≥ 1`; ослабление α2; wipe; goldens

## Report format

Files, counts, live case ids, gate table, what is not done and why.
