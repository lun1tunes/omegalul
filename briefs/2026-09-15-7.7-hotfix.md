# Task: 7.7 hotfix цикла агента (clip JSON, машинный чат, RAG status, kb_calls, mas_trace)

Read first: `AGENTS.md` (all), `MAS_REFACTORING_PLAN.md` §4 фаза 7, `.cursor/rules/mas-working-contract.mdc`.

## Goal
Закрыть пять дефектов фазы 7, которые гейт не ловит: (1) `clip(body,8000)` ломает JSON `get_keyword`; (2) Summarize AI steps шлёт машинный `agent.progress`; (4) Attach схлопывает abstain/failed в `unavailable`; (3) `kb_calls` считает стартовый Attach; (5) `mas_trace_case.py` читает `/log` без `status`. До 8.1.

## Out of scope
- 8.1 R1 / промпт `retrieve_knowledge` / полный текст карточек.
- Порядок событий RAG/LLM vs decision (п.6), `prompt_preview` (п.7), `presence_penalty` (п.8).
- `llm_unavailable`, `chat_extra_params`, mid-loop `service_unreachable`, удаление `n8n_agent`.
- Порог `rag_empty: 0` / `kb_calls ≥ 1` как красный гейт (Фаза 8).
- Goldens, wipe.

## Where
- `mas-agent-kit/mas_agent_kit/view.py` + `agent.py` `tool_view`: валидный JSON для LLM; роутер отдаёт `tool_view`, не сырой гигант.
- `n8n/templates/mas_agent_workflow.py`: Append без `clip(body,8000)`; Summarize `looksMachine` на `status_message`; Attach retrieve — тот же status mapper + `phase: on_demand`.
- `n8n/templates/mas_retrieval_client.py`: `retrievalOutage`; `failed` vs `abstain`; `phase` в payload.
- `mas-activity-service/app/case_log.py`: `kb_calls` = `phase=on_demand`; `log_record.status`/`actor`; `_quality_warning` на настоящий `failed`.
- `scripts/mas_trace_case.py`: timeline из log_record `status`/`actor`.
- `simulation-model-example/run_live_five.py`: `_assert_human_text` на `agent.progress`.
- smokes: excel/schedule/orchestrator; pytest kit + case_log.
- regenerate: agent workflows + orchestrator.
- `MAS_REFACTORING_PLAN.md` ревизия 47.

## Contracts to respect
- Инвариант 5: чат инженера без snake_case / `key=value` / JSON / `a|b`.
- Ответ инструмента — валидный JSON; ключ `error` запрещён.
- `abstain` (SCHEMA_KEYWORD_SCOPE_REQUIRED) ≠ `failed` (падение retrieval/Postgres).
- `kb_calls` не зеленеет от стартового Attach.
- α2 не ослаблять. `max_rag_empty`/`min_kb_calls` выключены.

## Evidence to start from
- `get_keyword` WCONHIST ~22K, clip на 8K → битый JSON с `…`.
- Live `CASE-6aa98a9e-c3b3a9`: чат «…в набор `exploitation_coefficients`».
- Live 12/12: Builder `kb_calls=1` при `rag.status=unavailable`, `retrieve_knowledge` не вызван.
- Attach: `failed=abstain||failed||needs_input||raw.error` → `unavailable`; `_quality_warning` ждёт `failed`, который не приходит.

## Done when
- [ ] Append tool result: `JSON.parse(content)` на ответе >8K; `details.parameters` на месте.
- [ ] Summarize: машинный финальный текст → «Агент завершил шаг.»; smoke CASE-6aa98a9e-c3b3a9.
- [ ] Attach error → `status=failed`; abstain → `status=abstain`; quality warning только failed.
- [ ] `kb_calls` считает только `phase=on_demand`.
- [ ] `GET /log` record несёт `status` и `actor`; mas_trace колонка status не пустая.
- [ ] `python3 scripts/mas_gate.py` GREEN
- [ ] `python3 scripts/mas_gate.py --live` GREEN 12/12
- [ ] План ревизия 47; шапка / §3 / §4 / §2.7 / «Ближайшие шаги» = код

## Forbidden
- regex-роутеры; домен в оркестраторе; правка JSON руками; `toolHttpRequest`; `$env`/`require`
- порог `rag_empty: 0`; ослабление α2; wipe; goldens
- clip по символам в n8n, дающий невалидный JSON

## Report format
Files changed, counts, live case ids, gate table, what is not done and why.
