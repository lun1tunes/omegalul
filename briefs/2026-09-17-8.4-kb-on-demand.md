# Task: KB по требованию в цикле агента (фаза 8.4 / R10 / S1)

Read first: `AGENTS.md` (весь), `MAS_REFACTORING_PLAN.md` §2.2 R10, §2.3 S1, §4 8.4, `.cursor/rules/mas-working-contract.mdc`, `mas-llm-first-no-domain-hardcode.mdc`, `n8n-templates.mdc`, `live-harness.mdc`.

## Goal

Цитата плана §4 / 8.4: `retrieve_knowledge` в цикле 7.3 для Builder (и Excel); промпт Builder'а: «перед первым `apply_*` по keyword, которого нет в текущих карточках retrieved knowledge, — `retrieve_knowledge`/`get_keyword`». On-demand отдаёт полный текст карточки (when-to-use + pitfalls); стартовый Attach остаётся `summary`.

*Готово:* live `excel_apply_dataset`: `trace.rag` WEFAC до `apply_dataset`; `kb_calls ≥ 1`; `tool_calls ≤ 4`.

## Out of scope

- 8.5 корпус (R6–R8), 8.6 оркестратор, 8.7 ingest/UI
- удаление `n8n_agent`
- S2 `schema_renderer` / `_tokens_from_fields`
- regex-роутер «запретить apply без retrieve»; порог `kb_calls` на commissioning
- goldens, wipe, ослабление α2 commissioning

## Where

- `n8n/templates/agents/schedule_builder.py` `SYSTEM` / `rag_ready_note`
- `n8n/templates/agents/excel_extractor.py` `SYSTEM` (retrieve, если протокола нет в срезе)
- `n8n/templates/mas_agent_workflow.py` `RETRIEVE_KNOWLEDGE_TOOL`, `js_prepare_retrieve` (запрошенные keyword — семьи среза, не хвост `slice(0,6)`), `js_attach_retrieve` (полный текст)
- `n8n/templates/mas_retrieval_client.py` `compactRetrievalCards(..., preferFull)`
- `n8n/tests/schedule-builder-agent-smoke.js`, `excel-extractor-agent-smoke.js`
- `simulation-model-example/run_live_excel_datasets.py` — `min_kb_calls=1`, `max_tool_calls_per_agent=4`, WEFAC в `trace.rag` до `apply_dataset`
- regen: `python3 scripts/mas_gate.py --only regen`

## Contracts to respect

`retrieve_knowledge` не в `trace.tool` / не в α2 `tool_calls`. `kb_calls` = `trace.rag` `phase=on_demand`. Ключ `error` в ответе инструмента запрещён. Текст задачи не выбирает keyword regex'ом. Поле: n8n 2.30.8 UI-only.

## Evidence to start from

R10/S1: стартовый срез Builder — первые `keyword_families` из инвентаря (INCLUDE/DATES/…), карточки `wefac-*` нет. `excel_apply_dataset` пишет WEFAC; `kb_calls: 0` до 8.4. `Prepare retrieve` мержил семьи и `slice(0,6)` мог отбросить запрошенный WEFAC.

## Done when

- [x] Prepare retrieve: `keywords=WEFAC` при полных начальных семьях → `keyword_families` содержит WEFAC; без `keywords` семьи on-demand пустые (поиск по query)
- [x] Attach retrieve: в tool-сообщении полный `text`, не обрезанный `summary`; `phase=on_demand`
- [x] SYSTEM Builder: retrieve → get_keyword → apply_dataset; commissioning без retrieve
- [x] `python3 scripts/mas_gate.py` GREEN
- [x] `--live` 12/12: apply `kb_calls ≥ 1`, WEFAC-карточка до `apply_dataset`, `tool_calls ≤ 4`; commissioning без обязательного `kb_calls`
- [x] план ревизия 52; шапка / §3 / §4 / §2.7 / «Ближайшие шаги» = код

## Forbidden

- regex-роутеры; домен в оркестраторе; правка JSON руками; `toolHttpRequest`; `$env`/`require`
- `min_kb_calls` на шести commissioning; ослабление α2 commissioning; wipe; goldens

## Report format

Files, counts, live case ids, gate table, what is not done and why.
