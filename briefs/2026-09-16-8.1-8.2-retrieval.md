# Task: Retrieval without false abstain + keyword summaries (8.1 + 8.2)

Read first: `AGENTS.md` (all), `MAS_REFACTORING_PLAN.md` §2.2 R1–R5/R14, §4 фаза 8 deliverables 8.1–8.2, `.cursor/rules/n8n-templates.mdc`.

## Goal
Schedule Builder gets knowledge on commissioning: `schedule_mvp` without `keyword_families` is ordinary retrieval, not `SCHEMA_KEYWORD_SCOPE_REQUIRED`. Tag branch searches query tokens; lexical uses Russian FTS; semantic keeps requested types; 45% RRF floor only on multi-branch cards; every `keyword_instruction` has `summary` ≤ 600 chars (when-to-use + pitfalls).

## Out of scope
- 8.3 R13 inventory filters
- 8.4 `retrieve_knowledge` prompt / `kb_calls ≥ 1`
- 8.5 corpus worked_example / Excel rewrite / stub routing cards
- 8.6 orchestrator query/topics/SYSTEM
- Deleting `n8n_agent` (ревизия 49)

## Where
- `n8n/templates/mas_knowledge_spaces.py`: KEYWORDS, NAMESPACES, SELECTORS (R14)
- `n8n/templates/schedule_rag_workflows.py`: attach, TAG/LEXICAL SQL, WRAP_SEMANTIC, ingest `summary`
- `n8n/templates/mas_retrieval_client.py`: compactRetrievalCards, orchestrator `text_limit` ≥ 1500
- `n8n/rag/excel-agent-operating-guide.documents.json`: `summary` + revision bump
- `n8n/tests/knowledge-retrieval-smoke.js` + `schedule-rag-runtime-smoke.js`
- live harness: `max_rag_empty=0` (not `min_kb_calls`)
- regenerate: `cd n8n/templates && python3 generate_schedule_workflows.py` (and agent gens via `mas_gate.py --only regen`)

## Contracts to respect
- `requireCoverage` unchanged; schema required only when keyword families were requested
- Tool JSON never carries key `error`
- No domain in orchestrator prompt; no regex routers
- Generate JSON from templates

## Evidence to start from
Builder Attach `SCHEMA_KEYWORD_SCOPE_REQUIRED` when `keyword_families=[]` (commissioning «сдвинуть даты ввода»). Live `rag_empty` on Builder, `CASE-6aa99f94-439404`.

## Done when
- [x] «даты ввода скважин» / schedule_mvp / no families → attach `succeeded`, ≥ 1 card
- [x] «коэффициент эксплуатации» tag tokens; WEFAC can come from tag branch
- [x] `WCONPROD` exact_hit; WRAP_SEMANTIC drops other types
- [x] 45% floor skipped for single-branch cards; `summary` used as card text; WCONPROD summary mentions WELTARG
- [x] all keyword_instruction cards have `summary` ≤ 600
- [x] `python3 scripts/mas_gate.py` GREEN
- [ ] `python3 scripts/mas_gate.py --live` 12/12 одним пулом — после первого прогона OpenRouter `Q-parse`/credits; commissioning `rag_empty: 0` (`CASE-6aaaeddc-*` + golden 2 `CASE-6aaaf133-55de14`)
- [x] plan header / §3 / §4 / §2.7 / nearest steps = ревизия 49

## Forbidden
- regex routers; hand-editing workflow JSON; machine text to the engineer; rewriting goldens; `kb_calls ≥ 1` as a red gate

## Report format
Files, counts, live case ids, gate table, what is not done and why.
