# Task: Корпус знаний (фаза 8.5 / R6 / R7 / R8)

Read first: `AGENTS.md` (весь), `MAS_REFACTORING_PLAN.md` §2.2 R6–R8, §4 8.5, `.cursor/rules/mas-working-contract.mdc`, `mas-llm-first-no-domain-hardcode.mdc`.

## Goal

Цитата плана §4 / 8.5: R6 (`worked_example` ×3), R7 (Excel-карточки как дельты; разморозить тесты), R8 (заглушки в `absent_agent`, `specialist_packet` убрать), проверка `summary` (8.2), карточка комментариев без чужих тегов.

*Готово:* тест корпуса: типы/теги/`summary`/нет снятых токенов (`clr_`, `continuation_state`, `specialist_packet`); live `trace.rag` шага 0 ⊆ четырёх policy-карточек.

## Out of scope

- 8.6 оркестратор (O24, O28–O30)
- 8.7 ingest `content_hash` / UI знаний
- удаление `n8n_agent`
- новые SCHEDULE keywords; PDF мануала
- goldens, wipe, ослабление α2

## Where

- `n8n/rag/excel-agent-operating-guide.documents.json` — корпус
- `n8n/templates/schedule_rag_workflows.py` — RRF не отдаёт `topics: absent_agent`, пока вызывающий не просил
- `n8n/templates/mas_knowledge_spaces.py` — SELECTORS.excel без вечного `clarification`
- `n8n/tests/knowledge-retrieval-smoke.js`, `excel-protocol-retrieval-boost-smoke.js`
- `excel-agent-tools/tests/test_workflow_contracts.py` — якоря R7, корпус
- `simulation-model-example/run_live_five.py` — шаг 0 ⊆ policy
- regen: `python3 scripts/mas_gate.py --only regen`; ingest: `POST /v1/knowledge/ingest`

## Contracts to respect

Оркестратор без домена. Карточки routing без имён агентов. `keyword_instruction.summary` ≤ 600. Ключ `error` в ответе инструмента запрещён. Поле: n8n 2.30.8 UI-only. Bump `revision` у изменённых id.

## Evidence to start from

R6: `worked_example` 0. R7: тесты замораживают `clr_` / `continuation_state` / `X-Excel-Webhook-Key`. R8: `route-cluster-calculation` и др. с тегом `маршрутизация` конкурируют на шаге 0. Комментарии `schedule-model-file-comments-v1` тегированы `INCLUDE`/`DATES`.

## Done when

- [ ] 3 `worked_example`; retrieval «сдвинь даты ввода» видит пример
- [ ] Excel protocol — дельты к SYSTEM; нет ops-карточки; нет `clr_` / `continuation_state` / `specialist_packet`
- [ ] заглушки `topics: [absent_agent]`; шаг 0 ⊆ {thin-orchestrator, excel, schedule, hitl-required-evidence}
- [ ] комментарии без чужих keyword
- [ ] `python3 scripts/mas_gate.py` GREEN
- [ ] `--live` 12/12; ingest новых ревизий
- [ ] план ревизия 53; шапка / §3 / §4 / §2.7 / «Ближайшие шаги» = код

## Forbidden

- regex-роутеры; домен в оркестраторе; правка JSON workflow руками
- `min_kb_calls` на commissioning; wipe; goldens
- 8.6 / 8.7 / удаление `n8n_agent`

## Report format

Files, counts, live case ids, gate table, what is not done and why.
