# Task: Оркестратор с политикой под цель (фаза 8.6 / O24 / O28 / O29 / O30)

Read first: `AGENTS.md` (весь), `MAS_REFACTORING_PLAN.md` §2.1 O24/O28–O30, §4 8.6, `.cursor/rules/mas-working-contract.mdc`, `mas-llm-first-no-domain-hardcode.mdc`.

## Goal

Цитата плана §4 / 8.6: O24, O28, O29, O30 — запрос без файлов/журнала, `topics` шага 0 из реестра, секции `planner_input`, абзац SYSTEM про политику, RAG для Verify (только политика завершения).

*Готово:* три разных цели → разные наборы `knowledge_id` (live-проверка в харнессе); smoke на `compactRetrievalCards` с одной веткой; `verify_input` содержит блок политики; smoke на порядок секций; `steps ≤ 6`; `warnings: 0`.

## Out of scope

- 8.7 ingest `content_hash` / UI знаний
- удаление `n8n_agent` (ревизия 54 после 8.6)
- O26 (unlisted wells)
- новые SCHEDULE keywords; PDF мануала
- goldens, wipe, ослабление α2
- карточки `absent_agent` на шаге 0, пока вызывающий их не просил (8.5)

## Where

- `n8n/templates/mas_state_utils.py` — `buildRetrievalQuery` (цель + план + открытые вопросы, без файлов и журнала); `planRetrievalTopics` (шаг 0: роли enabled агентов, чьи `input_required` покрыты приложенными ролями)
- `n8n/templates/mas_knowledge_spaces.py` — `SELECTORS.orchestrator.top_k` 12 → 20
- `n8n/templates/mas_retrieval_client.py` — секция «Политика из базы знаний» вместо JSON-хвоста; `topics` на карточках; 45 % для одноветочных, если вызывающий задал `topics` и карточка их не содержит
- `n8n/templates/generate_mas_orchestrator.py` — заголовки секций (константы), порядок Цель → План → Журнал → Данные агентов → Открытые вопросы → Доступные агенты → Политика; SYSTEM O30; `PREPARE_VERIFY` читает Attach, режет до «Данные агентов», добавляет карточки `topics: completion`
- `n8n/rag/excel-agent-operating-guide.documents.json` — `route-hitl-required-evidence` `topics` += `completion`, bump `revision`
- `n8n/tests/mas-orchestrator-smoke.js` — query/topics, порядок секций, `verify_input` политика, одна ветка
- `simulation-model-example/run_live_five.py` — `orch_rag_ids`; golden_1 ≠ golden_2; план следует карточкам
- `simulation-model-example/run_live_demo_agent.py` — file-less demo без пары excel+schedule карточек
- regen: `cd n8n/templates && python3 generate_mas_orchestrator.py` (+ excel/schedule/demo, если Attach)
- ingest: `POST /v1/knowledge/ingest` после bump ревизии HITL-карточки

## Contracts to respect

Оркестратор без домена. Routing-карточки без имён агентов. `error` в ответе инструмента запрещён. Поле: n8n 2.30.8 UI-only. Шаг 0 commissioning ⊆ {route-mas-thin-orchestrator, route-excel-extractor, route-schedule-builder, route-hitl-required-evidence}. `max_cards` 6, `text_limit` 1500.

## Evidence to start from

O24: query тащит имена файлов и summary агентов; `topics` на шаге 0 `[]`; RAG — JSON-хвост `Retrieved knowledge`. O28: `PREPARE_VERIFY` режет по «Текущее состояние» до Attach. O29: Цель → Журнал → План → JSON compact. O30: SYSTEM не говорит, что политика бьёт догадку. 8.5: live шаг 0 ⊆ четырёх карточек (`CASE-6aac4711-*`).

## Done when

- [ ] Query без файлов и журнала; шаг 0 topics из реестра (роли покрытых агентов)
- [ ] `planner_input` секции в фиксированном порядке; политика — именованный блок, не JSON
- [ ] Verify видит `topics: completion`; smoke на `verify_input`
- [ ] SYSTEM: политика > догадка; план по пунктам политики
- [ ] Три цели → разные `knowledge_id` (commissioning / rebind / file-less demo)
- [ ] `python3 scripts/mas_gate.py` GREEN
- [ ] `--live` 12/12; `steps ≤ 6`; `warnings: 0`; шаг 0 ⊆ четырёх policy
- [ ] план ревизия 54; шапка / §3 / §4 / §2.7 / «Ближайшие шаги» = код

## Forbidden

- regex-роутеры; домен в оркестраторе; правка JSON workflow руками
- `absent_agent` на шаге 0 без фильтра
- wipe; goldens; 8.7 / удаление `n8n_agent`

## Report format

Files, counts, live case ids, gate table, what is not done and why.
