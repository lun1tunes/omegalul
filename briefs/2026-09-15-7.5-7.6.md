# Task: Пороги llm_truncated/rag_empty/kb_calls в report.json и матрица секретов

Read first: `AGENTS.md` (all), `MAS_REFACTORING_PLAN.md` §4 фаза 7.5–7.6, §2.7 T3/T5/Q4/B9, `.cursor/rules/mas-working-contract.mdc`.

## Goal
Цитата плана §7 «Ближайшие шаги» п.1 и §4 7.5–7.6: `case_log.threshold_violations` + харнессы считают `llm_truncated` / `rag_empty` / `kb_calls`; гейт падает при `llm_truncated: 0` нарушении; `rag_empty` **считается**, порог `rag_empty: 0` **не** включать (Фаза 8 / R1). `docs.md`: «Матрица секретов и связей» (T3), «Что видно в логе»; полные `.env.example` (B9). `AGENTS.md` §4 события и §7 thinking.

## Out of scope
- R1 `SCHEMA_KEYWORD_SCOPE_REQUIRED` / Builder RAG `ready` (8.1).
- Порог `rag_empty: 0` и `kb_calls ≥ 1` как красный гейт (Фаза 8 / 12.2).
- B6 `llm_ms`/`tool_ms`, U7 группировка по `task_id`.
- 7.6 секретная матрица не меняет Runtime Config / credentials JSON.
- FastAPI агентов, оркестратор, RAG-ранжирование, goldens.

## Where
- `mas-activity-service/app/case_log.py`: `summarize` + `threshold_violations` (`max_llm_truncated=0`; `max_rag_empty`/`min_kb_calls` default None).
- `mas-activity-service/tests/test_case_log.py`, `tests/test_settings.py`.
- `simulation-model-example/run_live_five.py` (`attach_log_metrics`, `LIVE_JSON_KEYS`, `spec_threshold_kwargs`); demo/datasets берут те же функции.
- `docs.md`, `AGENTS.md`, `*.env.example` (Activity + Excel + Schedule + Math + Demo + корневой lab).
- `mas-activity-service/static/log.js` + `?v=` — счётчики в сводке лога.
- `MAS_REFACTORING_PLAN.md` ревизия 46.

## Contracts to respect
- α2: `warnings: 0`, `steps ≤ 6`, `tool_calls ≤ 4` не ослаблять.
- Пустой Builder RAG (`unavailable`) **не** α2-warning (уже так).
- `retrieve_knowledge` не входит в `tool_calls` (уже в цикле агента).
- `kb_calls` = `trace.rag` агента (caller/agent_id ≠ orchestrator).
- `llm_truncated`: `trace.llm` `finish_reason=length` или `llm_parse.error=llm_truncated_empty` / `finish_reason=length` на решении.
- Секреты не в JSON workflows.

## Evidence to start from
Ревизия 45: `--repeat 3` GREEN, `llm_truncated: 0` как факт лога, порога харнесса не было. Builder `trace.rag` `unavailable` + `SCHEMA_KEYWORD_SCOPE_REQUIRED` на commissioning (`CASE-6aa97921-*`).

## Done when
- [ ] `GET /cases/{id}/log` `summary` несёт `llm_truncated`, `rag_empty`, `kb_calls` (и by_agent где уместно).
- [ ] `threshold_violations` падает при `llm_truncated > 0`; `max_rag_empty`/`min_kb_calls` выключены по умолчанию.
- [ ] `_live_five_out/report.json` (и demo/datasets reports) содержат эти ключи на каждом результате.
- [ ] `test_settings`: aliases `Settings` ⊆ `mas-activity.env.example`.
- [ ] `docs.md` без «Chat Model» у агентов; есть матрица и «Что видно в логе».
- [ ] `python3 scripts/mas_gate.py` GREEN
- [ ] `python3 scripts/mas_gate.py --live` GREEN 12/12, `llm_truncated: 0` в строках харнесса (`--hot` ок: JSON не меняется)
- [ ] `MAS_REFACTORING_PLAN.md` ревизия 46; шапка / §3 / §4 / §2.7 / «Ближайшие шаги» = код

## Forbidden
- regex-роутеры; домен в оркестраторе; правка JSON руками; `toolHttpRequest`; `$env`/`require`
- порог `rag_empty: 0` в харнессе; ослабление α2; wipe; goldens
- секреты в репозитории

## Report format
Files changed, counts, live case ids, gate table, what is not done and why.
