# Task: Цикл агента на HTTP `/chat/completions` с thinking off (фаза 7.3 + 7.4)

Read first: `AGENTS.md` (весь), `MAS_REFACTORING_PLAN.md` §2.7 Q1–Q4, §2.2 R10, §2.3 X7, §4 Фаза 7 deliverables 7.3 и 7.4, §7 «Ближайшие шаги» п.1. Правила: `.cursor/rules/mas-working-contract.mdc`, `field-deployment-constraints.mdc`, `mas-llm-first-no-domain-hardcode.mdc`, `n8n-templates.mdc`. Скилл после правок: `/mas-gate`, при живом кейсе `/mas-trace CASE-…`.

Нужна сильная модель: n8n 2.30.8 (цикл по рёбрам, Code без `require`, HTTP Request 4.4, `$('Node').last`/`item` после HTTP), двойники спека TOOLS → OpenAI `tools`.

## Goal

Цитата плана §4 / «Ближайшие шаги» п.1: цикл агента на HTTP `/chat/completions` с thinking off и профилями сэмплинга (флаг `llm_transport`), `retrieve_knowledge` как виртуальный инструмент цикла, результат агента — из сессии (X7). Критерий: `--live` 12/12 без `lmChatOpenAi` в core-JSON агентов, `trace.llm` без `<think>`, `llm_truncated: 0`; таблица сравнения профилей сэмплинга (`--repeat 3`).

Зачем: на поле `lmChatOpenAi` 1.3 не умеет выключить thinking Qwen3.6 (`/no_think` не поддерживается). Decision уже на HTTP. Агенты должны тем же транспортом. Пока агенты на Chat Model, правки промпта/RAG недоказуемы (`role=agent` нет).

Критерий сессии: live commissioning в логе имеет `trace.llm` `role=agent` с `finish_reason` и токенами, `content_preview` без `<think>`; JSON Excel/Schedule/Demo без `@n8n/n8n-nodes-langchain.agent` и `lmChatOpenAi`; α2 сохранены.

## Out of scope

- Фаза 7.5–7.6: порог `rag_empty: 0` в харнессе; полная «Матрица секретов»; полные `.env.example`. `llm_truncated` в `report.json` харнесса — 7.5; здесь — `finish_reason=length` → `trace.llm` warn (уже в `case_log`) и проверка live, что таких нет.
- Фаза 8: R1 abstain Builder, 45 % RRF, русский FTS, корпус. `retrieve_knowledge` вызывает тот же retrieval; пустой Builder срез на commissioning без keyword — ожидаемый факт (как 7.1).
- Фаза 9+: pydantic kit, discovery, LongJob.
- Python FastAPI Excel/Builder/demo (`app/agent.py`, `agent_tools.py`) — не трогать. X7 только в n8n `Summarize`/`Format`/`Fetch`.
- `SYSTEM` оркестратора, guards, план, Verify-логика. Спеки агентов: TOOLS/промпты/файлы `agents/*.py` не переписывать (кроме поля `llm_transport` с дефолтом, если нужно).
- Goldens, wipe, ослабление α2 (`warnings: 0`, `steps ≤ 6`, `tool_calls ≤ 4`). `retrieve_knowledge` не считать в `tool_calls` α2 (kit `trace.tool` его не пишет).
- Бюрократия B8.

## Where

- `n8n/templates/llm_runtime_options.py` — `CHAT_THINKING_OFF` += `chat_template_kwargs: {enable_thinking: false}`. `SAMPLING = {decision: {temperature: 0.2, top_p: 0.9}, agent: {temperature: 0.7, top_p: 0.8, top_k: 20, presence_penalty: 1.5}}`. Не слать `max_tokens`.
- `n8n/templates/generate_mas_orchestrator.py` — `BUILD_DECISION_CHAT` / `BUILD_VERIFY_CHAT` / `BUILD_INTERPRET_CHAT`: вместо `temperature:0` — `...SAMPLING.decision` + новый THINKING_OFF. JSON `response_format` оставить.
- `n8n/templates/mas_agent_workflow.py` — флаг `llm_transport` (`http_loop` по умолчанию, `n8n_agent` откат на одну ревизию). При `http_loop`:
  - нет `langchain.agent` / `lmChatOpenAi` / per-tool `httpRequestTool`;
  - граф: Restore RAG → `Build chat request` → `{Title} Agent chat` (HTTP 4.4, credential Qwen, `chat_url` + `chat_request` как Decision) → `Parse agent chat` → Activity `trace.llm` role=agent → Restore → Switch `loop_route` (`chat`/`tool`/`retrieve`/`done`);
  - `tool` → Prepare + `Call agent tool` HTTP `POST {service}/agent-tools/{name}` `{session_id, …args}` → Append → Switch;
  - `retrieve` → Prepare `schedule_retrieval_request` → Execute Workflow Knowledge Retrieval → Attach compact cards → Activity `trace.rag` → Append как `role=tool` → Switch;
  - `chat` → снова Build; `done` → Summarize → tools Activity → Fetch `/sessions/{id}/result` всегда (X7, без `hasResult` по префиксу инструмента) → Format → Close.
  - `tools` в chat_request = OpenAI function schema из `spec.tools` (json-поля — `string`) + виртуальный `retrieve_knowledge {query, keywords?}`.
  - Сообщения цикла на item (`messages[]`); HTTP сносит item — Parse/Append читают предыдущую Code-ноду через `.item`/`.last()`.
  - Срез `<think>…</think>` из content перед логом и перед `messages`.
- `n8n/templates/mas_agent_spec.py` — опционально `llm_transport: str = "http_loop"`; файлы `agents/*.py` не обязаны меняться.
- `scripts/lab_soft_redeploy.py` — HTTP Request с credential `openAiApi` (Agent chat) получает lab Qwen id; сейчас патч завязан на тип `lmChatOpenAi` или имя с `qwen` (имя REPLACE уже содержит Qwen — проверить).
- `mas-activity-service/app/case_log.py` — `trace.llm` `length` уже warn; не считать α2-warning пустой RAG. Не добавлять `rag_empty: 0` в харнесс.
- Регенерация: `python3 scripts/mas_gate.py --only regen` (или generators + `generate_schedule_workflows.py` последним). JSON руками не править.

Тесты / смоки (case id в docstring)

- `n8n/tests/excel-extractor-agent-smoke.js`, `schedule-builder-agent-smoke.js`, `demo-agent-smoke.js` — граф цикла: лимит итераций → `done`; ветка `stop`; ветка `tool_calls`; `retrieve_knowledge`; нет `lmChatOpenAi`/`langchain.agent`; OpenAI `tools[].function.name` = TOOLS спеки + `retrieve_knowledge`. Summarize/Format: X7 всегда fetch; человеческий `llm_final_text` при пустой сессии → `needs_input` прозой.
- `excel-agent-tools/tests/test_workflow_contracts.py` — Demo/Excel/Schedule JSON без Chat Model; схема инструментов = `spec.tools`; убрать ожидание `httpRequestTool` / `Demo Agent AI Agent` для http_loop.
- `n8n/tests/mas-orchestrator-smoke.js` — тело Decision: `chat_template_kwargs.enable_thinking === false`, `temperature === 0.2`, нет `max_tokens`.
- `n8n/tests/mas-error-traces-smoke.js` — упавший узел = `{Title} Agent chat` (HTTP), не langchain.agent.
- Не добавлять в live-харнесс `rag_empty: 0`.

Документы

- `AGENTS.md` §4 события: `trace.llm` role=agent; §6 новый агент без Chat Model; §7 ловушка thinking — агенты тоже HTTP.
- `docs.md` — credential на **Agent chat** (не Chat Model у агентов); Decision/Verify/Interpret без изменения смысла.
- `MAS_REFACTORING_PLAN.md` — Ревизия 45: Q1/Q2/Q4(agent)/R10(часть, инструмент в цикле без живых wefac-карточек)/X7. Шапка, §3, §4, §2.7, «Ближайшие шаги». Журнал 2–44 не переписывать. Таблица профилей сэмплинга по `--repeat 3` в §7.

## Contracts to respect

Поле: n8n 2.30.8 UI-only; сервисы `.venv`+pip; адреса Runtime Config; секрет — Credentials; Code без `require`/`$env`; не `toolHttpRequest`; HTTP Request 4.4. После UI-импорта биндить Qwen credential на **Agent chat** каждого агента (как Decision chat).

`agent_result` не менять. Ошибка инструмента — `{ok:false, code, message}` без ключа `error`. Человеку — русская проза. `trace.*` не в чат.

Цикл: `max_iterations` из спеки (сейчас 8). После LLM-хода с `tool_calls` инструменты этой пачки выполнить даже на последней итерации, следующий chat не слать.

`execRef()` на `trace.llm` агента.

## Evidence to start from

Ревизия 44, `CASE-6aa95bf0-c2a4a1`: оркестратор `trace.llm` decision/verify есть; агентских `role=agent` нет. Builder `#228529` `rag.status=unavailable`. Q1: Excel/Schedule workflow `langchain.agent` 3.1 + `lmChatOpenAi` 1.3 (`mas_agent_workflow.py` 451–478). Q2: нет `chat_template_kwargs`. X7: `js_summarize` `hasResult` по `extract_`/`ask_engineer` (`mas_agent_workflow.py` 307–314). Decision HTTP — образец: `openai_chat_http`, `jsonBody: ={{ $json.chat_request }}`, `chat_base_url + /chat/completions`.

## Done when

- [ ] При `http_loop` в JSON Excel/Schedule/Demo нет `lmChatOpenAi` и `langchain.agent`; есть HTTP Agent chat + Switch цикла + `retrieve_knowledge` в schema.
- [ ] `test_workflow_contracts`: OpenAI tools = `spec.TOOLS` + `retrieve_knowledge`.
- [ ] X7: Fetch всегда; пустая сессия + человеческий текст модели → `needs_input` этим текстом, не стоковый вопрос из-за имени инструмента.
- [ ] Decision/Verify/Interpret body: три выключателя thinking + `SAMPLING.decision`.
- [ ] `python3 scripts/mas_gate.py` GREEN.
- [ ] `python3 scripts/mas_gate.py --live` GREEN 12/12, α2. В логе commissioning: `trace.llm` role=agent без `<think>`; `finish_reason=length` нет (llm_truncated: 0 как факт лога). Полный import n8n (не `--hot`, JSON меняется).
- [ ] `--live --repeat 3` GREEN; таблица steps/tool_calls/время в Ревизии 45. Стартовые SAMPLING; менять значения только если repeat красный по α2 — зафиксировать итог в §7.
- [ ] План ревизия 45; `AGENTS.md` / `docs.md` credential Agent chat.

## Forbidden

- regex/keyword heuristics вместо решения LLM; домен в промпте оркестратора.
- ручной JSON; `toolHttpRequest`; `$env`, `require`, community nodes.
- машинный текст инженеру; инженер пишет `.INC`/JSON.
- перепись goldens; wipe; ослабление α2.
- лечить thinking через `reasoningEffort` на lmChatOpenAi.

## Report format

Files changed, counts, case ids of the live run, gate table, `--repeat 3` table, what is not done and why.
